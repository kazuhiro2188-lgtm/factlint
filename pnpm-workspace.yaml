packages:
  - "packages/*"
  - "apps/*"

# vitest が依存する esbuild は、インストール時にネイティブバイナリを取得する
# ビルドスクリプトを持つ。pnpm はこれを既定で実行せず、明示的な承認が無いと
# インストール自体を失敗させる（ERR_PNPM_IGNORED_BUILDS）。
allowBuilds:
  esbuild: true
